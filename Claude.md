## Environment

- OS: Ubuntu (WSL). Use Linux paths and bash.
- Python: use a virtual environment (venv or conda). Pin the version you actually use (e.g. 3.10 or 3.11).
- Look and Install deps from the project’s dependency file (`requirements.txt`, `pyproject.toml`, etc.).

## Project

- **Hair_Style_Recommendation**: hair-style recommendation from images (or as defined in this repo’s README).
- Prefer small, reviewable changes. Match existing folder layout and naming.

